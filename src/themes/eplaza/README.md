# Eplaza Storefront Theme

Eplaza is a Tiwlo ecommerce storefront theme for electronics-style catalogs, product search, cart, checkout, compare, and order tracking.

## Run Locally

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
